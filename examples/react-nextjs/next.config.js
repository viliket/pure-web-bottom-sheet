/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  distDir: "dist",
  basePath: "/pure-web-bottom-sheet/react-nextjs",
  trailingSlash: true,
};
module.exports = nextConfig;
